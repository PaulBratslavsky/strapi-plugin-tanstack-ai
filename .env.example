# Copy to .env (gitignored) for the browser tests: `npm run test:e2e`.
# An admin on the Strapi the tests run against (http://localhost:1360).
# Only used when the stored session in e2e/.auth/ is missing or expired.
STRAPI_ADMIN_EMAIL=
STRAPI_ADMIN_PASSWORD=
